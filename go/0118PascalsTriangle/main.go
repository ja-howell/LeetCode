func generate(numRows int) [][]int {
    if numRows == 1 {
        return [][]int{{1}}
    }
    row := []int{}
    prevRow := []int{1,1}
    triangle := [][]int{{1},{1,1}}
    for i := 0, i <= numRows-2; i++ {
        row = slices.append
    }
}